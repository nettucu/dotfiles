# Repository-provided chezmoi defaults.
# Keep machine-specific overrides in ~/.config/chezmoi/chezmoi.toml.
{{- $role := promptStringOnce . "role" "Machine role [workstation/server]" }}
{{- $ageRecipient := promptStringOnce . "ageRecipient" "Age public key (recipient, leave blank if unknown)" }}

encryption = "age"
sourceDir = {{ .chezmoi.sourceDir | quote }}

[data]
  role = {{ $role | quote }}
  ageRecipient = {{ $ageRecipient | quote }}

[age]
  identity = "~/.config/age/key.txt"
  recipient = {{ $ageRecipient | quote }}
